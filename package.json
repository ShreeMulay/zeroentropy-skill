{
  "name": "zeroentropy-skill",
  "version": "1.1.5",
  "description": "ZeroEntropy agent skill for embeddings, reranking, and search",
  "private": true,
  "scripts": {
    "test": "pytest tests/",
    "lint": "markdownlint-cli2 SKILL.md README.md recipes/*.md",
    "typecheck": "tsc --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --skipLibCheck examples/typescript/*.ts"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "markdownlint-cli2": "^0.13.0",
    "typescript": "^5.4.0",
    "zeroentropy": "0.1.0-alpha.10"
  }
}
