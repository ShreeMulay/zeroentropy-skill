# Change: v1.1.4 Codebase Review Remediation

## Status
completed

## Description
Address verified findings from the full zeroentropy-skill codebase review. This release hardens the OpenCode plugin runtime behavior, aligns SDK/API contracts, fixes packaging and release reproducibility, and corrects docs/examples that could mislead users into failed installs or premature querying.

## Beads
- Epic: `zeroentropy-skill-9h8`
- H1: `zeroentropy-skill-gvd` — plugin runtime and API-contract hardening
- H2: `zeroentropy-skill-jgl` — packaging/version/CI/release alignment
- M1: `zeroentropy-skill-kbb` — docs/examples/schema corrections

## Impact
- `plugin/src/index.ts` — lazy client creation, retry parsing, SDK retry coordination, search params/output, permission gate, schema validation, async indexing status.
- `plugin/tests/` — red-first coverage for runtime import behavior, retry env parsing, endpoint params, schema validation, permissions, status semantics.
- `package.json`, `plugin/package.json`, `package-lock.json`, `plugin/package-lock.json`, `skill.json`, `SKILL.md`, `CHANGELOG.md` — version/release alignment.
- `.woodpecker.yml`, `.github/workflows/release.yml` — deterministic CI and release validation/assets.
- `README.md`, `plugin/README.md`, `examples/`, `recipes/`, `schemas/` — corrected install/config, async indexing, latency, and filter guidance.

## Test Plan (TDD)
- Add failing plugin tests before implementation for:
  - Plugin import succeeds without `ZEROENTROPY_API_KEY`; tool call returns structured missing-key error.
  - Invalid `ZEROENTROPY_MAX_RETRIES` defaults safely and does not skip API calls.
  - `zeroentropy_search` forwards endpoint-correct params and returns `document_results` for pages/snippets.
  - `zeroentropy_delete_collection` calls `context.ask` before deletion.
  - `zeroentropy_index` returns pending/accepted semantics, not `indexed`.
  - Real Zod schemas reject invalid dimensions, invalid `k`, invalid `top_n`, empty batch documents, and single-string `texts` unless explicitly supported.
- Add failing root validation tests before implementation for:
  - All version sources match `1.1.4`.
  - Plugin lockfile exists for `npm ci`.
  - OpenCode config examples use singular `plugin`.
  - Metadata-filter schema accepts numeric comparison values.
  - Standalone indexing examples do not claim immediate query readiness.

## Acceptance Criteria
- [x] Plugin build and Vitest suite pass.
- [x] Root pytest validation suite passes.
- [x] TypeScript examples typecheck.
- [x] Woodpecker-compatible CI uses deterministic installs.
- [x] Release workflow validates all version sources and includes full skill assets.
- [x] Live API smoke test passes when `ZEROENTROPY_API_KEY` is available.
- [x] Beads issues are closed, OpenSpec archived, changes committed and pushed to Forgejo then GitHub.
